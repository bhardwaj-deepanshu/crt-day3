#include <stdio.h>
int main()
{
  int n, fact = 1;
  printf("enter a no.");
  scanf("%d",&n);
  while (n)
  {
    fact *= n;
    n--;
  }
  printf("factorial of n is : ");
  printf("%d",fact);
  return 0;
}
